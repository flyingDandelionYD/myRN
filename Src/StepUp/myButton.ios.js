import React,{Component} from 'react';
import {
    Button,
} from 'react-native';

export  default class MButton extends  Component{
    render() {
        return(
            <Button color='red'
                    title='IOSButton'>
            </Button>
        )
    }
}
